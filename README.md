# Getting Started

1. Install yarn
```
brew install npm
npm install --global yarn
yarn --version
```

2. Install other dependencies
```
npm --install react-scripts
```

3. Start local server
```
yarn start
```
